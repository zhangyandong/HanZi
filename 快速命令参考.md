# ⚡ 快速命令参考

## 🎯 最常用命令

```bash
# 🔥 开发
yarn dev

# 🚀 部署
yarn deploy

# 🧪 测试
yarn deploy:preview
```

---

## 📋 完整命令列表

### 开发命令

```bash
# 启动开发服务器（http://localhost:3000）
yarn dev

# 构建生产版本
yarn build

# 预览生产版本（http://localhost:4173）
yarn preview
```

### 部署命令

```bash
# 一键部署到 Vercel（构建+部署）⭐
yarn deploy

# 仅部署到 Vercel（不重新构建）
yarn deploy:vercel

# 部署到 Netlify（构建+部署）
yarn deploy:netlify

# 本地预览（构建+预览，不部署）
yarn deploy:preview
```

---

## 🎯 使用场景

### 场景 1：日常开发
```bash
yarn dev
# 开发中...
# 保存文件自动刷新
```

### 场景 2：完成功能，准备上线
```bash
# 先测试
yarn deploy:preview
# 在浏览器测试 http://localhost:4173

# 确认无误，部署
yarn deploy
```

### 场景 3：紧急修复 Bug
```bash
# 修改代码
# ...

# 直接部署
yarn deploy
# 2-3分钟后修复上线
```

### 场景 4：测试 PWA
```bash
yarn deploy:preview
# 访问 http://localhost:4173
# F12 → Application → Service Workers
```

---

## 📊 命令对比

| 命令 | 速度 | 用途 | 推荐度 |
|------|------|------|--------|
| `yarn dev` | ⚡ 最快 | 开发 | ⭐⭐⭐⭐⭐ |
| `yarn deploy` | 🐢 较慢 | 部署 | ⭐⭐⭐⭐⭐ |
| `yarn deploy:preview` | 🚶 中等 | 测试 | ⭐⭐⭐⭐ |
| `yarn deploy:vercel` | ⚡ 快 | 重部署 | ⭐⭐⭐ |

---

## 💡 小技巧

### 技巧 1：部署前自动测试
```json
// package.json
{
  "scripts": {
    "predeploy": "yarn deploy:preview",
    "deploy": "..."
  }
}
```

### 技巧 2：查看部署状态
```bash
# Vercel 控制台
open https://vercel.com/dashboard
```

### 技巧 3：快速重启开发服务器
```bash
# 停止：Ctrl+C
# 重启：↑ 键 + Enter（调用上一条命令）
```

---

## 🎊 一键操作

### 最简单的部署流程

```bash
# 确保在项目目录
cd /Users/ts/Desktop/HanZi

# 一键部署
yarn deploy
```

**等待 2-3 分钟，完成！** ✅

---

## 📞 需要帮助？

**查看详细文档**：
- `docs/部署脚本使用说明.md` - 脚本详解
- `docs/部署指南.md` - 完整部署说明
- `docs/PWA测试步骤.md` - PWA 测试方法

---

**记住这三个命令就够了**：

```
yarn dev            # 开发
yarn deploy:preview # 测试
yarn deploy         # 部署
```

🚀 简单高效！

