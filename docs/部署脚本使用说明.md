# 🚀 部署脚本使用说明

## 📋 可用脚本

package.json 中已添加以下部署脚本：

---

## 🎯 快速部署命令

### 主要命令

#### 1. 一键部署到 Vercel（推荐）⭐

```bash
yarn deploy
```

**功能**：
- ✅ 自动构建生产版本
- ✅ 自动部署到 Vercel
- ✅ 使用固定项目名：hanzi-learning
- ✅ 一条命令完成所有操作

**等同于**：
```bash
yarn build && npx vercel --prod --yes --name hanzi-learning
```

---

#### 2. 仅部署（不重新构建）

```bash
yarn deploy:vercel
```

**适用场景**：
- 已经运行过 `yarn build`
- 只想重新部署
- 节省构建时间

---

#### 3. 部署到 Netlify

```bash
yarn deploy:netlify
```

**功能**：
- 自动构建
- 部署到 Netlify
- 指定 dist 目录

---

#### 4. 本地预览（测试部署）

```bash
yarn deploy:preview
```

**功能**：
- 构建生产版本
- 在本地预览
- 测试 PWA 功能
- 不实际部署

---

## 📊 命令对比

| 命令 | 构建 | 部署平台 | 适用场景 |
|------|------|---------|---------|
| `yarn deploy` | ✅ | Vercel | 🌟 最常用 |
| `yarn deploy:vercel` | ❌ | Vercel | 快速重部署 |
| `yarn deploy:netlify` | ✅ | Netlify | 备选方案 |
| `yarn deploy:preview` | ✅ | 本地 | 测试验证 |

---

## 🔄 完整部署流程

### 方式一：一键部署（最简单）

```bash
# 一条命令搞定
yarn deploy
```

**完整过程**：
```
1. TypeScript 编译
2. Vite 构建
3. PWA 文件生成
4. 上传到 Vercel
5. 部署成功
6. 获得访问链接
```

---

### 方式二：分步部署（可控）

```bash
# 步骤 1：构建
yarn build

# 步骤 2：本地测试
yarn preview
# 访问 http://localhost:4173 测试

# 步骤 3：确认无误后部署
yarn deploy:vercel
```

---

### 方式三：测试后部署

```bash
# 1. 构建并预览
yarn deploy:preview

# 2. 测试完成后，部署
yarn deploy:vercel
```

---

## 💻 实际使用示例

### 场景 1：第一次部署

```bash
cd /Users/ts/Desktop/HanZi

# 一键部署
yarn deploy

# 等待 2-3 分钟...
# ✅ Production: https://hanzi-learning-xxx.vercel.app
```

---

### 场景 2：代码更新后重新部署

```bash
# 修改了代码...

# 重新部署
yarn deploy

# 新版本自动上线
```

---

### 场景 3：部署前先测试

```bash
# 构建并预览
yarn deploy:preview

# 在浏览器测试 http://localhost:4173
# 确认无误

# 停止预览（Ctrl+C）

# 部署上线
yarn deploy:vercel
```

---

### 场景 4：快速重部署（不重新构建）

```bash
# 已经构建过了

# 直接部署
yarn deploy:vercel

# 节省构建时间
```

---

## 🎯 最佳实践

### 推荐工作流

**日常开发**：
```bash
yarn dev           # 开发模式
```

**功能完成后**：
```bash
yarn deploy:preview  # 预览测试
```

**测试通过后**：
```bash
yarn deploy        # 部署上线
```

---

## ⚡ 效率对比

### 传统方式
```bash
yarn build                      # 手动构建
npx vercel --prod --yes --name hanzi-learning  # 手动部署
```

**步骤**：2步  
**时间**：需要记住长命令  
**易错**：容易忘记参数

### 脚本方式
```bash
yarn deploy                     # 一键完成
```

**步骤**：1步 ✨  
**时间**：快速简单  
**易错**：不会出错

---

## 🔧 自定义脚本

如果需要添加其他部署脚本：

### 编辑 package.json

```json
{
  "scripts": {
    "deploy:github": "yarn build && gh-pages -d dist",
    "deploy:custom": "yarn build && scp -r dist/* user@server:/path"
  }
}
```

### 添加预部署检查

```json
{
  "scripts": {
    "predeploy": "yarn lint && yarn test",
    "deploy": "yarn build && vercel --prod"
  }
}
```

**`predeploy` 会在 `deploy` 前自动运行**

---

## 📋 所有可用脚本

### 开发脚本
```bash
yarn dev           # 启动开发服务器
```

### 构建脚本
```bash
yarn build         # 构建生产版本
yarn preview       # 预览生产版本
```

### 部署脚本
```bash
yarn deploy              # 一键部署到 Vercel
yarn deploy:vercel       # 仅部署（不构建）
yarn deploy:netlify      # 部署到 Netlify
yarn deploy:preview      # 本地预览测试
```

---

## 🎊 快速参考

### 最常用命令

```bash
# 日常开发
yarn dev

# 部署上线
yarn deploy

# 本地测试
yarn deploy:preview
```

就这三个命令，简单好记！

---

## ✅ 脚本添加完成

现在你可以：

**开发时**：
```bash
yarn dev
```

**部署时**：
```bash
yarn deploy
```

**测试时**：
```bash
yarn deploy:preview
```

---

**现在试试运行 `yarn deploy` 一键部署吧！** 🚀

或者先运行 `yarn deploy:preview` 在本地测试 PWA 功能！

详细说明请查看 `docs/部署脚本使用说明.md`

